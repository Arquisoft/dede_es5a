import React from 'react'
import { CartActionReducer, CartProduct } from "../shared/shareddtypes";

const cartReducer = (state:CartProduct[], action:CartActionReducer) => {
    return state;  
}

export default cartReducer;